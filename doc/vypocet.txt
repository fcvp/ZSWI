foreach (obor.typ[])										  //pole oborů pro daný typ studia
	foreach (klíčový termín v oboru[])							  //každé klíčové slovo související s oborem z pole
		if (isset(Get[".."] && GET[".."] !=0.5){
			diference = Math.abs (isset(Get[".."]) - db.getValue(ID_oboru, ID_slova)) //vezme hodnotu zadaného slova a porovná s hodnotou v db
			pocet++;								  //potřebné pro dělení
			}
	procenta = (1-(diference/pocet))*100

----

hodnoceni = 0   -> dif = 0
hodnoceni = 0.5 -> 